#include<stdio.h>
void main(){
    int a,b,area,volume;
    printf("enter the value of cube :");
    scanf("%d",&a);
    area=6*a*a;
    printf("area of cube is %d \n:",area);
    printf("enter the value of cube :");
    scanf("%d",&b);
    volume=b*b*b;
    printf("volume of cube is %d :",volume);
}